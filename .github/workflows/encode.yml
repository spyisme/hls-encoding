name: Distributed HLS Cluster Encoder (Using AES)

on:
  repository_dispatch:
    types: [trigger-encode]

jobs:
  build-and-transcode:
    runs-on: ubuntu-latest
    steps:
      - name: Validate Required Secrets
        env:
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_ENDPOINT: ${{ secrets.R2_ENDPOINT }}
          R2_BUCKET_NAME: ${{ secrets.R2_BUCKET_NAME }}
        run: |
          for var in R2_ACCESS_KEY_ID R2_SECRET_ACCESS_KEY R2_ENDPOINT R2_BUCKET_NAME; do
            if [ -z "${!var}" ]; then
              echo "❌ CRITICAL: Missing GitHub Secret: $var"
              exit 1
            fi
          done
          echo "✅ All required repository secrets are present."

      - name: Checkout Codebase
        uses: actions/checkout@v4

      - name: Setup Node Environment
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Install System Dependencies (FFmpeg, Rclone, yt-dlp)
        run: |
          sudo apt-get update -q
          sudo apt-get install -y ffmpeg
          curl -s https://rclone.org/install.sh | sudo bash
          sudo curl -sL https://github.com/yt-dlp/yt-dlp/releases/latest/download/yt-dlp -o /usr/local/bin/yt-dlp
          sudo chmod a+rx /usr/local/bin/yt-dlp

      - name: Install Node Dependencies
        run: |
          npm init -y
          npm install fluent-ffmpeg

      - name: Configure Rclone Credentials
        env:
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_ENDPOINT: ${{ secrets.R2_ENDPOINT }}
        run: |
          mkdir -p ~/.config/rclone
          cat <<EOF > ~/.config/rclone/rclone.conf
          [r2]
          type = s3
          provider = Cloudflare
          access_key_id = $R2_ACCESS_KEY_ID
          secret_access_key = $R2_SECRET_ACCESS_KEY
          endpoint = $R2_ENDPOINT
          acl = private
          EOF

      - name: Configure GDrive Credentials (If needed)
        if: github.event.client_payload.source_type == 'gdrive'
        env:
          GDRIVE_SA_JSON: ${{ secrets.GOOGLE_SERVICE_ACCOUNT_JSON }}
        run: |
          echo "$GDRIVE_SA_JSON" > /tmp/gdrive-sa.json
          chmod 600 /tmp/gdrive-sa.json
          cat <<EOF >> ~/.config/rclone/rclone.conf

          [gdrive]
          type = drive
          service_account_file = /tmp/gdrive-sa.json
          scope = drive.readonly
          EOF

      - name: Download Processing Target File
        env:
          SOURCE_TYPE: ${{ github.event.client_payload.source_type }}
          FILE_ID: ${{ github.event.client_payload.gdrive_file_id }}
          ZOOM_URL: ${{ github.event.client_payload.zoom_url }}
          VIDEO_URL: ${{ github.event.client_payload.video_url }}
        run: |
          if [ "$SOURCE_TYPE" = "gdrive" ]; then
            rclone backend copyid gdrive: "$FILE_ID" ./gdrive_download -P
            mv ./gdrive_download/* input.mp4
            rm -rf ./gdrive_download
          
          elif [ "$SOURCE_TYPE" = "zoom" ]; then
            yt-dlp --no-playlist --merge-output-format mp4 -o "input.mp4" "$ZOOM_URL"
          
          else
            curl -L "$VIDEO_URL" -o input.mp4
          fi

          if [ ! -f input.mp4 ]; then
            echo "❌ ERROR: File download failed. input.mp4 not found."
            exit 1
          fi
      - name: Run Transcoder with AES-128 Encryption
        id: transcode
        env:
          KEY_URL: ${{ github.event.client_payload.key_url }}
        run: |
          cat <<'EOF' > run-encode.js
          const { execSync } = require('child_process');
          const fs = require('fs');
          const path = require('path');
          const crypto = require('crypto');
          
          const input = 'input.mp4';
          const outDir = './output/media';
          const keyUrl = process.env.KEY_URL || 'fallback.key';
          fs.mkdirSync(outDir, { recursive: true });

          // 1. Generate AES-128 Key
          const rawKey = crypto.randomBytes(16);
          const hexKey = rawKey.toString('hex');
          const keyPath = path.resolve('enc.key');
          const keyInfoPath = path.resolve('key_info.txt');

          fs.writeFileSync(keyPath, rawKey);
          fs.writeFileSync(keyInfoPath, `${keyUrl}\n${keyPath}\n`);
          fs.appendFileSync(process.env.GITHUB_ENV, `VIDEO_HEX_KEY=${hexKey}\n`);

          // 2. Ultra-Fast Thumbnail Generation
          try {
            execSync(`ffmpeg -ss 00:02:00 -i "${input}" -vframes 1 -q:v 2 ./output/thumbnail.jpg -y`, { stdio: 'ignore' });
          } catch (e) {
            execSync(`ffmpeg -i "${input}" -vframes 1 -q:v 2 ./output/thumbnail.jpg -y`, { stdio: 'ignore' });
          }

          // 3. Fast Probe for Native Height
          let nativeHeight = 1080;
          try {
            const probeResult = execSync(`ffprobe -v error -select_streams v:0 -show_entries stream=height -of default=nw=1:nk=1 "${input}"`).toString().trim();
            nativeHeight = parseInt(probeResult, 10) || 1080;
            console.log(`📏 Detected native video height: ${nativeHeight}p`);
          } catch (e) {
            console.warn("⚠️ Probe failed, defaulting to 1080p pipeline max.");
          }

          // 4. Dynamically Build Target Tiers
          const activeTiers = [];
          if (nativeHeight >= 1080) {
            activeTiers.push({ name: '1080p', h: 1080, w: 1920, vb: '4500k', buf: '9000k', ab: '128k', crf: 23 });
          }
          if (nativeHeight >= 720) {
            activeTiers.push({ name: '720p', h: 720, w: 1280, vb: '2500k', buf: '5000k', ab: '96k', crf: 24 });
          }
          // Always include at least 480p (or native if it's smaller than 480p)
          activeTiers.push({ name: '480p', h: 480, w: 854, vb: '900k', buf: '1800k', ab: '64k', crf: 26 });

          // 5. Construct FFmpeg Command Strings
          const splitCount = activeTiers.length;
          let filterComplex = "";
          let videoMaps = "";
          let audioMaps = "";
          let streamMap = "";

          if (splitCount > 1) {
            filterComplex += `[0:v]split=${splitCount}`;
            for (let i = 0; i < splitCount; i++) filterComplex += `[v${i}]`;
            filterComplex += `; `;
            for (let i = 0; i < splitCount; i++) {
              filterComplex += `[v${i}]scale=w=${activeTiers[i].w}:h=${activeTiers[i].h}:force_original_aspect_ratio=decrease[v${i}out]`;
              if (i < splitCount - 1) filterComplex += `; `;
            }
          } else {
            // Single output fallback prevents FFmpeg crash on `split=1`
            filterComplex = `[0:v]scale=w=${activeTiers[0].w}:h=${activeTiers[0].h}:force_original_aspect_ratio=decrease[v0out]`;
          }

          for (let i = 0; i < splitCount; i++) {
            videoMaps += `-map "[v${i}out]" -c:v:${i} libx264 -preset superfast -crf ${activeTiers[i].crf} -maxrate ${activeTiers[i].vb} -bufsize ${activeTiers[i].buf} `;
            audioMaps += `-map 0:a -c:a:${i} aac -b:a:${i} ${activeTiers[i].ab} `;
            streamMap += `v:${i},a:${i},name:${activeTiers[i].name} `;
          }

          console.log(`🚀 Starting 1-Pass Transcode for: ${activeTiers.map(t => t.name).join(', ')}...`);
          
          const ffmpegCmd = `ffmpeg -threads 0 -i "${input}" \\
            -filter_complex "${filterComplex}" \\
            ${videoMaps} \\
            ${audioMaps} \\
            -f hls -hls_time 6 -hls_playlist_type vod \\
            -hls_key_info_file "${keyInfoPath}" \\
            -hls_segment_filename "${outDir}/%v_%03d.ts" \\
            -master_pl_name master.m3u8 \\
            -var_stream_map "${streamMap.trim()}" \\
            "${outDir}/%v.m3u8"`;

          try {
            execSync(ffmpegCmd, { stdio: 'inherit' });
            
            // Move master.m3u8 to output root
            fs.renameSync(path.join(outDir, 'master.m3u8'), './output/master.m3u8');

            // Secure local key cleanup
            if (fs.existsSync(keyPath)) fs.unlinkSync(keyPath);
            if (fs.existsSync(keyInfoPath)) fs.unlinkSync(keyInfoPath);

            console.log("\n✅ Transcode and Packaging Complete!");
          } catch (err) {
            console.error("❌ FFmpeg execution failed:", err);
            process.exit(1);
          }
          EOF

          START_TIME=$(date +%s)
          node run-encode.js
          END_TIME=$(date +%s)
          echo "ELAPSED_SECONDS=$((END_TIME - START_TIME))" >> $GITHUB_ENV

      - name: Deploy Assets to Cloudflare R2
        env:
          PARENT_FOLDER: ${{ github.event.client_payload.parent_folder }}
          OUTPUT_NAME: ${{ github.event.client_payload.output_name }}
          BUCKET: ${{ secrets.R2_BUCKET_NAME }}
        run: |
          # Handle empty parent_folder cleanly
          if [ -n "$PARENT_FOLDER" ]; then
            TARGET_PATH="$BUCKET/$PARENT_FOLDER/$OUTPUT_NAME"
          else
            TARGET_PATH="$BUCKET/$OUTPUT_NAME"
          fi

          rclone copy ./output "r2:$TARGET_PATH" \
            --transfers 16 --checkers 32 --fast-list -P

      - name: Cleanup Service Account
        if: always() && github.event.client_payload.source_type == 'gdrive'
        run: rm -f /tmp/gdrive-sa.json

      - name: Trigger Webhook Callback
        if: always()
        env:
          WEBHOOK_URL: ${{ github.event.client_payload.callback_webhook }}
          OUTPUT_NAME: ${{ github.event.client_payload.output_name }}
          JOB_STATUS: ${{ job.status }}
        run: |
          if [ -n "$WEBHOOK_URL" ]; then
            # Convert GitHub's "Success"/"Failure" to lowercase for APIs
            STATUS=$(echo "$JOB_STATUS" | tr '[:upper:]' '[:lower:]')
            
            curl -X POST "$WEBHOOK_URL" \
              -H "Content-Type: application/json" \
              -d "{
                \"status\": \"$STATUS\",
                \"output_name\": \"$OUTPUT_NAME\",
                \"time_taken_seconds\": \"${ELAPSED_SECONDS:-0}\",
                \"hex_key\": \"${VIDEO_HEX_KEY:-}\"
              }"
          else
            echo "No webhook URL provided. Skipping callback."
          fi
